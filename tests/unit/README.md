# Unit suite manifest

The aggregate unit command discovers and executes the production contract
fixtures under `tests/architecture`, `tests/config`, `tests/document`,
`tests/layout`, `tests/persistence`, `tests/selections` and `tests/workbench`.
