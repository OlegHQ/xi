# T057 — Implement Git discovery, status and diff

## Current status — reopened 2026-09-15

**Unproven / todo.** The report qualifies porcelain parsing/cache only and defers diff rendering; actual temporary-repository comparison and E11 diff-selection UX remain unproven.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T057 — Implement Git discovery, status and diff
- Outcome: passed for porcelain-v2 status discovery/cache
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T057-source-manifest.sha256](T057-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2
- Specifications: `docs/plan/04-services.md` Git status contract

## Observable result

`parsePorcelainV2Z` decodes machine-readable NUL-delimited Git status records, retaining staged/unstaged/conflicted state, rename source paths, untracked/ignored entries and repository generation. `GitStatusCache` rejects late refreshes and never treats unsaved text as a stageable disk state.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/git/t057-status.test.ts` | Passed porcelain-v2 parsing for modified/added/rename/conflict/untracked/ignored records, stable path identity and stale cache rejection. | `tests/git/t057-status.test.ts`; fixtures `T057-PORCELAIN-01`, `T057-STALE-01`, `T057-FAIL-01` |
| `bun x tsc --noEmit` | Passed strict repository typecheck. | Command output |
| `bun run check:public-boundary` | Passed. | Command output |
| `git diff --check` | Passed. | Command output |

## Performance and visual evidence

Parsing is a single pass over bounded protocol records and cache publication stores immutable metadata. Diff rendering remains a downstream UI owner.

## Failure cases and recovery

Malformed UTF-8/records return typed failures. Stale generations cannot replace a newer snapshot. Conflict records are explicit and cannot be mistaken for resolved text.

## Limitations and next action

Process-backed Git commands, hunk/diff models and mutation commands are owned by T058/T059/T072.

## 2026-09-19 maintenance addendum — Git row click preview

A primary click on a Git file row now routes through the existing diff-preview
path instead of opening the plain file and closing the Git panel. The Git panel
remains docked while the temporary INDEX/WORKTREE diff occupies the editor area.
This fixes the click regression only; T057 remains todo under the broader
acceptance statement above.

- `bun test tests/git/git-panel.test.ts`: passed pointer diff routing, target
  selection and docked-panel preservation.
- `python3 tests/e2e/git-panel-pty.py`: passed a real SGR mouse click in a
  disposable repository, observed `XI_GIT_DIFF_OPEN`/`READY`, verified no
  `XI_GIT_PANEL_CLOSED`, closed the diff and staged the next selected file.
- `bun run test:ui`: passed all 20 UI fixtures.
- `bun run check`, `git diff --check`, and `python3 tools/plan.py check`: passed.
- Real xterm capture visually inspected at
  `.artifacts/ui/t045-screenshots/git-click-diff-fix.png`; Git remains selected
  and visible beside the rendered unified diff.
- No reference-host latency qualification is claimed; T115 retains that gate.

## 2026-09-20 maintenance addendum — editable whole-file diff

Git diff activation now opens the ordinary document buffer and paints current-file additions
plus deletion anchors as editor decorations. The old hunk-only surface no longer covers the
editor, so normal Vim editing and the existing syntax-span pipeline remain active; `]c`/`[c`
still use the diff controller, and a docked Git panel uses `Ctrl-W` to transfer focus.

- `bun test tests/ui/git-diff.test.ts tests/workbench/t116-input-router.test.ts tests/ui/t087-motion-paint.test.ts tests/ui/t053-syntax-paint.test.ts`: passed controller decoration and editable-focus regressions alongside syntax/motion paint.
- `bun run test:ui`: passed all 20 UI fixtures.
- `python3 tests/e2e/git-diff-pty.py`: passed the real CLI path, whole-file activation and hunk navigation without the old read-only projection.
- `bun run check` and `git diff --check`: passed.
- Removed lines are represented by a gutter anchor on the nearest surviving current-file row;
  Xi does not create a second writable text store for deleted virtual text.
- T057 remains `todo`; this maintenance slice does not claim its complete status/diff/layout
  acceptance or T115's reference-host performance qualification.

## 2026-09-20 correction — comparison editor, not file decorations

The preceding whole-file addendum describes a superseded implementation. Its PTY
check did not establish editing/saving: an attempted edit check failed and was
omitted. The current test restores that assertion and verifies exact saved bytes.

Implemented distinct comparison views/tabs sharing the working document and undo
owner, immutable original syntax snapshots, service-owned full-file alignment,
visible-only layout projection, split/unified painting, `+/-` gutters, first-change
focus, hunk navigation and asynchronous version-checked live diffs. Original/index
text and alignment rows have no editable hit targets. A narrow terminal no longer
opens an unfocused passive Explorer overlay over the comparison.

Environment: Linux arm64, Bun 1.3.13, TypeScript 7.0.2, OpenTUI 0.5.11; shared
uncommitted worktree. No ticket status change or release-performance claim.

Executed validation:

- `bun run check`: passed strict types, ownership/import DAG and lint.
- `bun run test:ui`: passed all 20 fixtures.
- `bun test tests/ui/git-diff.test.ts tests/workbench/t116-input-router.test.ts tests/git/diff-model.test.ts tests/git/diff-service.test.ts`: passed separate tabs, tab switching, shared document/undo, first-change cursor, split/unified deleted and current text, syntax, edit/close preservation, index read-only keys, snapshot cleanup, line-diff edge cases and real Git service cases. The service fixture now awaits its async body, preventing premature test completion.
- `python3 tests/e2e/git-diff-pty.py`: passed actual CLI edit/save with spaces and literal brackets, hunk navigation and comparison close.
- `python3 tests/e2e/git-panel-pty.py`: passed real mouse-opened index comparison, close/focus restoration and staging the selected file in a disposable repository.
- `python3 tests/e2e/git-diff-visual.py`: passed real xterm wide-to-narrow resize and exact saved TypeScript bytes. Initial execution correctly failed save because Biome was absent; the layout fixture now supplies the existing `XI_FORMATTER_COMMAND=/bin/cat` injection. Formatter behavior is not qualified by this fixture.

Visually inspected `.artifacts/ui/git-comparison/wide.png`, `edited.png`,
`narrow.png` and `saved.png`: separate `(Working Tree)` tab, first change at
line 31, syntax on both sides, aligned red/green rows and filler, unified deleted
rows, unsaved labeling and clean state after saving. Captures are local artifacts.

Remaining qualification: reference-host input/startup/open p50/p95/p99/max,
physical latency, allocation/RSS and loaded-system comparisons are unmeasured.
Cooperative diff computation has bounded Myers work/backtracking and cancellation,
but string splitting/snapshot materialization and comparison painting still need
production-path budget measurement. Full T057 failure coverage (external index
changes, missing/deleted worktree files, nested repositories and missing Git) is
not established by this slice. Intraline emphasis, comparison-session persistence,
and multi-pane comparison composition are not claimed as VS Code parity.
