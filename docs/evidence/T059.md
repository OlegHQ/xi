# T059 — Implement merge and history navigation

## Current status — reopened 2026-09-15

**Unproven / todo.** The report explicitly defers real-repository merge orchestration and diff hunk UI; E10 final file/unmerged-state and history focus journeys remain unproven.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T059 — Implement merge and history navigation
- Outcome: passed for read-only history/conflict state owner
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T059-source-manifest.sha256](T059-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2
- Specifications: `docs/plan/04-services.md` merge/history contract

## Observable result

`GitHistoryController` publishes immutable history entries and opens them as read-only records. Conflict state retains base/ours/theirs/result identities, and resolution requires Git’s unmerged state to be clear; marker-looking text alone never marks a conflict resolved.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/git/t059-history.test.ts` | Passed read-only history opening, unresolved marker rejection and explicit Git-state resolution. | `tests/git/t059-history.test.ts`; fixtures `T059-HISTORY-01`, `T059-CONFLICT-01`, `T059-CONFLICT-02` |
| `bun x tsc --noEmit` | Passed strict repository typecheck. | Command output |
| `bun run check:public-boundary` | Passed. | Command output |
| `git diff --check` | Passed. | Command output |

## Performance and visual evidence

History/conflict records are bounded metadata snapshots. Diff rendering remains a separate UI owner.

## Failure cases and recovery

Unknown history IDs return explicit unavailable. Conflict resolution refuses while Git reports unmerged entries. Disposal clears history/conflict state.

## Limitations and next action

Real repository merge orchestration and diff hunk UI remain downstream qualification work; mutation commands are T058.
