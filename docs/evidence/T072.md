# T072 — Implement explicit Git branch, remote and stash commands

## Current status — reopened 2026-09-15

**Unproven / todo.** Completion is withdrawn because prerequisite acceptance was reopened: T058. Existing component evidence is retained; this is not a newly observed defect in this ticket's implementation.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T072 — Implement explicit Git branch, remote and stash commands
- Outcome: passed
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T072-source-manifest.sha256](T072-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2
- Specifications: `docs/plan/04-services.md` explicit Git operation contract

## Observable result

The Git mutation owner now exposes validated branch checkout, stash push/pop/list and remote fetch/pull/push actions. Every operation is explicit, argv-based and generation-checked; commit/refresh never invokes network actions implicitly.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/git/t072-remote-branch.test.ts` | Passed branch/stash/fetch argv validation and explicit no-automatic-push behavior. | `tests/git/t072-remote-branch.test.ts`; fixtures `T072-NET-01`, `T072-FAIL-01` |
| `bun x tsc --noEmit` | Passed strict repository typecheck. | Command output |
| `bun run check:public-boundary` | Passed. | Command output |
| `git diff --check` | Passed. | Command output |

## Performance and visual evidence

Operations are serialized background commands; no shell interpolation or render-path process launch occurs.

## Failure cases and recovery

Invalid branch/remote names are rejected. Generation changes block mutation. Executor failures remain recoverable typed errors and cancellation is owned by the process port.

## Limitations and next action

A real remote endpoint and network cancellation matrix are release qualification responsibilities; this ticket only defines explicit command semantics.
