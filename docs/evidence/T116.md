# Ticket evidence

- Ticket ID and title: T116 Restore thin CLI and owned editing-session orchestration
- Outcome: structural acceptance passed (all gates green); STARTUP-WARM/STARTUP-COLD remain diagnostic-only per the probe classification, and the ticket stays `todo` because dependency T106 is not done
- Implementation revision or tree hash: working tree on top of 56a9074 (pre-existing uncommitted changes preserved; pre-refactor snapshot `git stash create` = 615bf9c6015880d01661fcbdd5945b732f09e16f)
- Environment and pinned dependency/oracle versions: Bun per package.json engines, TypeScript 7.0.2, OpenTUI 0.5.11, Linux 6.8
- Specification sections and acceptance items covered: 01-architecture ownership table (apps/xi = construct/wire/start/stop only), 13-performance-research rows for `apps/xi/src/main.ts`

## Observable result

Structural refactor with no intended behavior change. Work is split into serial, independently reviewable slices:

| Slice | Scope | Status |
|---|---|---|
| S1 | `packages/vim/multi.ts` → `packages/vim/multi/index.ts`; `packages/workbench/selection-persistence.ts` → `packages/workbench/session/`; dead `workspaces` field removed from package.json | done, `bun run check` passes |
| S2 | Split `packages/layout/src/index.ts` (2 045 lines) into `types`, `packed-index`, `graphemes`, `shaping`, `viewport`; `index.ts` is a 32-line barrel with the same public surface | done; `bun run check`, tests/layout t014/t085/t111 pass; `bench/layout/t014-viewport.ts` cacheStats byte-identical before/after, p95 within run-to-run noise (job tmp s2-bench-before/after.txt) |
| S3 | Split `packages/workbench/vim-session/index.ts` (2 085 lines) into `types`, `host-commands`, `selection-commands`, `pointer`, `insert-plan`, `helpers`; `createOwnedVimSession` closure kept intact (1 459-line index) | done; `bun run check`, tests/workbench scripts, `test:unit`, t064 editor/vim PTY pass |
| S4 | `packages/workbench/host/` `BufferHost` (documents/sessions maps, `createSession`, `openBufferAtPath`, preview discard, panel exclusivity registry, surface-change broadcast, dispose) and `packages/workbench/picker/` `PickerController`/`ThemeController` (generic over the UI theme type; theme id persistence through a narrow filesystem port). `main.ts` 3 571 → 3 293 lines | done; `bun run check`, `test:unit` (38 fixtures incl. new t116-buffer-host / t116-picker-controller), t064 editor/file PTY, t039 picker PTY + test, t045 e02 picker preview, keyboard-only journey, t132 theme PTYs, t038 session, `test:startup` pass |
| S5 | `packages/workbench/explorer/` `ExplorerController` (open/close, filtering, pending-`g`, rename/copy/delete drafts, in-memory undo journal, pointer/context-menu activation, tree-refresh marker forwarding). `main.ts` 3 293 → 2 991 lines | done; `bun run check`, `test:unit` (39 fixtures incl. new t116-explorer-controller), t040 Explorer component test, t045-e03 explorer filter/reveal PTY, t131 explorer file-management PTY, t045 keyboard-only-journey PTY, t064 editor PTY, `test:startup` pass |
| S6 | `packages/workbench/search/` `SearchController` (open/close, query flags/selection, replace-input draft, `createReplacePort`/`applyReplacement`, buffer-text cache for the search backend's dirty-buffer source). `main.ts` 2 991 → 2 691 lines | done; `bun run check`, `test:unit` (40 fixtures incl. new t116-search-controller), t043 search PTY, t044 replace PTY, t045-e05 rapid-typing PTY, t045-e06 replace-dirty-closed PTY, t045-e15 degraded-missing-rg PTY, t129 panel-scroll-picker-search PTY, t045 keyboard-only-journey PTY, t064 editor PTY, `test:startup` pass |
| S7 | `packages/workbench/problems/` `ProblemsController` (Problems panel open/close/selection/keypress; Output panel open/close/keypress; `.xi/tasks.toml` discovery+cache, running a configured task, and publishing its matched problems into the shared `DiagnosticStore` under a task-specific serverId, all sharing the task-diagnostic generation counter). `main.ts` 2 691 → 2 558 lines | done; `bun run check`, `test:unit` (41 fixtures incl. new t116-problems-controller), t049 problems PTY, t060 tasks PTY, t127 live-diagnostics PTY, t045 keyboard-only-journey PTY, t064 editor PTY, t045-g3 test, `test:startup` pass |
| S8 | Outline/hover/completion/signature/snippet/workspace-edit state → `packages/workbench/language/` (`overlays.ts` `LanguageOverlayController`; `completion.ts` `CompletionSnippetController`; `workspace-edits.ts` `WorkspaceEditsController`). `main.ts` 2 558 → 1 807 lines | done; `bun run check`, `test:unit` (44 fixtures incl. new t116-language-overlays / t116-completion-controller / t116-workspace-edits), t050 outline / t051 completion / t052 workspace / t071 native-navigation / t088 completion-failures / t088 multi-snippet PTYs (live typescript-language-server), t045-e16 narrow-popup-resize / t055 formatting / t045 keyboard-only-journey PTYs, t064 editor PTY, `bun test` t069-completion / t071-native-navigation / t056-g4, `test:startup` pass |
| S9 | Input routing (`packages/workbench/input/router.ts` `WorkbenchInputRouter`), pointer/splitter/context-menu routing (`packages/workbench/input/pointer-router.ts` `WorkbenchPointerRouter`), Ex/host/window command handling (`packages/workbench/commands/host-commands.ts` `WorkbenchHostCommands`) and save/format/checkpoint (`packages/workbench/editing/save-coordinator.ts` `SaveCoordinator`) out of `main()`; new `ARCH-COMPOSITION-ROOT-01` architecture contract. `main.ts` 1 807 → 1 121 lines | done; `bun run check` (incl. `ARCH-COMPOSITION-ROOT-01`), `test:unit` (48 fixtures incl. new t116-input-router / t116-pointer-router / t116-host-commands / t116-save-coordinator), t064 editor/vim/ex/file PTYs, t045-e02/e11/e12/e13 PTYs, t055 formatting PTY, t127 panel-pointer + panel-pointer-picker PTYs, t128-e22 splitter-xterm PTY, t129 context-menu + mouse-toggle PTYs, t130 dot-repeat/macro/change-repeat PTYs, t045 keyboard-only-journey PTY, t071 native-navigation PTY, t038 session test, t045-g3 test, `test:startup` pass |
| S10 | Full re-validation: `bun run check`, `test:unit` (48), `test:ui` (9), `test:services` (34), `test:e2e` (68 fixtures, full pass on rerun), `test:startup`; paired startup/key-output probes against the pre-refactor snapshot; `main.ts` 3 571 → 1 114 lines | done; see validation and performance sections |

Pre-existing defect found during S4 (not introduced, reproduced against the pre-S4 `main.ts`): `tests/e2e/t039-picker-pty.py` hangs roughly 1 run in 4–8 because an in-flight async picker file-preview open can resolve after the picker was cancelled, leaving an orphaned session so the next `q` closes a view instead of quitting. Needs a generation guard on the async open path; behavior change, tracked as follow-up, not fixed in this structural ticket.

S5 judgment calls: `ExplorerController` takes the real `BufferHost` and the workbench's own `WorkbenchSession` (as a narrow structural `ExplorerSessionPort` -- `activeViewId`/`views`/`buffer`/`buffers`/`renameBufferPath`, all already workbench-owned types), plus narrow ports it declares itself: `ExplorerTreePort`/`ExplorerNavigationPort` (mirroring `packages/services/files`' `ExplorerTree`/`ExplorerNavigationController`, bound once via a new `attachTree()` after the composition root constructs them -- the tree/controller are still constructed lazily in `main.ts`'s `initializeOptionalServices`, unchanged) and `ExplorerFileOperationsPort` (mirrors the platform filesystem's `stat`/`renamePath`/`copyPath`/`removePath`/`makeDirectory`, passed the real `NodeFilesystemPort` instance from `main.ts`). Stderr reporting goes through a new `onError` port instead of the controller calling `process.stderr` itself, matching `BufferHost`'s existing marker-port convention. The lazy "services not loaded yet" gate at `onKeypress` keeps checking `explorerTree === undefined` in `main.ts` (smallest diff, per the ticket's own suggested options) rather than exposing that check through the controller. Disposal: the previous `explorerCancellation.dispose()` call site (mid-teardown, alongside `explorerController?.dispose()`/`explorerTree?.dispose()`) now calls `explorerFeature.dispose()`, which also clears the pending-`g` timer -- previously cleared separately at the very end of teardown via a standalone `clearExplorerPendingG()`. Both are idempotent, order-independent cleanups (a timer clear and a cancellation dispose, neither observed by any other teardown step), so moving the timer clear earlier changes no observable behavior; this is a deliberate exception to "same disposal order" for two calls that were never actually dependent on their position. `createExplorerFilesystem`/`toExplorerEntry`/`toExplorerWatchEvent`/`relativeWorkspacePath`/`toExplorerFailure` stayed in `main.ts` (composition/adapter code per the ticket's own allowance), since they touch platform + services types workbench cannot import.

Design rule for S4–S9: each feature controller lives in `packages/workbench/<feature>/`, holds the long-lived state and key handling that `main()` held, and depends only on narrow port interfaces it declares itself (or on `packages/contracts`). `apps/xi` constructs services and passes them structurally; it never re-implements a handler. Workbench must not import `packages/services` (enforced by `tools/check-import-graph.ts`).

S6 judgment calls: `SearchController` takes the real `BufferHost` and the workbench's own `WorkbenchSession` (as a narrow structural `SearchSessionPort` -- `buffers`/`applyDocumentEdits`), plus narrow ports it declares itself: `SearchFilesystemPort` (mirrors the platform filesystem's `workspaceRelativePath`/`workspaceAbsolutePath`/`readFile`/`writeFileAtomic`) and a full set of structural mirrors of `packages/services/search`'s public types (`WorkbenchSearchQuery`/`WorkbenchSearchMatch`/`WorkbenchSearchModel`/`WorkbenchSearchBufferSource`/`WorkbenchReplaceTarget`/`WorkbenchReplacementEdit`/`WorkbenchReplacePlan`/`WorkbenchReplaceJournal(Entry)`/`WorkbenchReplaceFailure`/`WorkbenchReplaceApplyResult`) plus `SearchServicePort`/`ReplaceServicePort` (the subset of `RealtimeSearchService`/`WorkspaceReplaceService` the controller actually calls) and `WorkbenchReplaceApplyPort` (the shape `createReplacePort()` returns, mirroring services' `ReplaceApplyPort`, consumed by `main.ts`'s real `WorkspaceReplaceService`). Service *construction* (`RealtimeSearchService`, `RipgrepSearchBackend`, `WorkspaceReplaceService`) stays in `main.ts`'s `initializeOptionalServices`, unchanged; a new `attachServices(search, replace, applyReplacementEdits)` (mirroring S5's `attachTree`) binds the constructed instances plus the `applyReplacementEdits` free function into the controller and starts forwarding search results as `XI_SEARCH_RESULT` markers -- the controller owns that subscription (disposed by its own `dispose()`, which also clears the dirty-buffer text cache) rather than `main.ts` holding a separate `searchSubscription` variable, so the single `searchFeature.dispose()` call replaces the old `searchSubscription?.dispose()` teardown line in the same position, preserving disposal order. `createReplacePort()`/`readReplaceTarget` keep the exact original split: a dirty open buffer is read from `BufferHost.documents` and, on apply, mutated only through `SearchSessionPort.applyDocumentEdits` (the document/edit coordinator); a closed file goes only through `SearchFilesystemPort.readFile`/`writeFileAtomic`. `workspaceRoot` is passed into the controller as a plain string value (never re-reads `process.cwd()`), and all former `process.stderr.write` calls in the moved functions (including the replace-count success message) go through the injected `onError` port, matching `ExplorerController`'s convention. `asUndoGroupId` is not re-exported from `packages/contracts` (only the `UndoGroupId` type is), so the controller constructs undo-group IDs with `asIdentifier<UndoGroupId>(value, 'undoGroupId')` from contracts instead of importing `packages/primitives` directly -- functionally identical, since `asUndoGroupId` is itself defined as exactly that delegation, and importing primitives directly from workbench is rejected by `tools/check-import-graph.ts`'s enforced graph (which is narrower here than this ticket's own architecture-document wording). `fullDocumentText`/`textHash` (pure helpers over `DocumentSnapshot`/`Uint8Array`, no forbidden imports) are duplicated as small private functions inside `search/index.ts` rather than removed from `main.ts`, since `main.ts` still uses its own copies for unrelated (language/formatter/snippet) code paths untouched by this slice.

S7 judgment calls: `ProblemsController` owns two panels (Problems, Output) plus the configured-tasks feature that connects them, because task output and task-sourced diagnostics are one feature sharing state (`taskDiagnosticGeneration`, the configured-tasks cache) across both -- splitting them into separate files/controllers per the ticket's own "split only if clearly cleaner" allowance would have meant threading that shared state across a controller boundary for no benefit. The `DiagnosticStore` instance itself (`diagnostics`) is *not* attach-later like S5/S6's lazily-constructed services: it is constructed eagerly in `main.ts` before any feature controller (it is also written to by the language feature's LSP diagnostics, so it can never become problems-exclusive) and is simply passed into `ProblemsController`'s options at construction, exactly like `session`/`filesystem` are passed to `ExplorerController` -- no `attachServices` needed for it. `TaskController` is different: its construction is genuinely deferred until the first task actually runs (not "as soon as a panel's services are ready"), so instead of an `attachServices`-style bind, the controller receives an `ensureTaskController: () => Promise<TaskControllerPort>` callback -- mirroring the existing `ensureServices`/`ensureFormatting` on-demand-construct-and-memoize pattern already used for the explorer/search panels' own service loading, and functionally equivalent to "construction stays in `main.ts`, bound to the controller" per the ticket's own wording, since `main.ts` still owns the `let taskController` variable, the dynamic import, the `new tasksModule.TaskController(...)` call and the one-time `.subscribe(() => host.notifySurfaceChange())` wiring, memoized behind that callback. `main.ts` keeps that same `taskController` local afterward purely for `get output()`'s `read:`/existence-check wiring (identical to how `searchService`/`explorerTree` remain `main.ts` locals for their own `get search()`/`get explorer()` getters even though the controllers also hold their own references) -- the controller caches its own copy inside `runConfiguredTask` (needed synchronously by `handleOutputKeypress`'s cancel-key check and by `:taskstop`'s `cancelTask()`), so there are two references to the same object, never two sources of truth. Task discovery (`readTasksConfig`) and the generic-compiler problem matcher (`matchTaskProblems`) are passed as plain function values from `main.ts` rather than importing `packages/services/config`/`packages/services/tasks` types into workbench (forbidden), matching S6's `applyReplacementEdits` precedent; `matchTaskProblems` stays synchronous rather than returning a `Promise` (which would have reordered the `diagnostics.publish(...)` calls relative to the `XI_TASK_EXITED` marker write, an observable behavior change) by having `main.ts` cache the dynamically-imported tasks module the first time either `ensureTaskController` or `readTasksConfig` loads it, so it is already resident by the time a task can reach its terminal state and trigger the matcher. `fileUri`/`workspacePathFromUri` (from the services-owned `launch-core` entrypoint) are likewise passed as plain function values, matching the same constraint that already forced `SearchController`'s `applyReplacementEdits` and `ExplorerController`'s filesystem port to be function/structural values rather than imported types. Per the ticket, the `:task`/`:tasks`/`:taskstop` command *dispatch* (the regex matches and the `command === '...'` checks) stays in `main.ts`'s `handleWorkbenchCommand`; only the body each branch calls (`problemsFeature.runConfiguredTask(...)`, `.listConfiguredTasks()`, `.cancelTask()`) moved. All former `process.stderr.write` calls inside the moved functions (task-discovery-invalid-config, no-tasks-configured, configured-tasks-list, unknown-task) now go through the injected `onError` port with byte-identical message strings, matching S5/S6's convention; `handlePanelPointer`/`openPanelContextMenu`'s problems branch, the `sidebar.git` control activation and the three leader-key sites (`p`, `d`/`e` panel-prefix and non-prefix) now call `problemsFeature.openProblems()`/`.setSelectedProblemIndex()`/`.openProblem()` instead of the removed `main()`-local functions, with no other change to those call sites.

S8 judgment calls: the language feature split into three controllers exactly as the ticket suggested (`overlays.ts`'s `LanguageOverlayController` for outline+hover; `completion.ts`'s `CompletionSnippetController` for completion+signature+the shared snippet-session lifecycle; `workspace-edits.ts`'s `WorkspaceEditsController` for code actions/rename/workspace-proposal-apply/unopened-and-open-file access), plus a shared `overlays.ts`-exported `buildNavigationRequest` pure function (the original `currentNavigationRequest`) all three controllers call with their own injected `session`/`fileUri`, since every language request starts from the same "where is the cursor" read. Unlike the explorer/search/problems ports, the language-service classes/types listed in the ticket (`LanguageServerSession`, `LanguageNavigationController`, `CompletionController`, `LanguageServerCompletionProvider`, `SignatureController`, `LanguageServerSignatureProvider`, `LanguageServerWorkspaceEditProvider`, `WorkspaceEditCoordinator`, `WorkspaceEditResourceExecutor`, `SnippetSession`, `expandSnippet`, `executeLanguageCodeAction`, the completion/snippet/workspace-edit DTOs, `NavigationRequest`, `HostLocation`) are all mirrored as structural ports (`NavigationControllerPort`, `CompletionControllerPort`/`CompletionProviderPort`, `SignatureControllerPort`, `WorkspaceEditProviderPort`, `SnippetSessionPort`/`SnippetSessionCtor`/`ExpandSnippetFn`, `ExecuteLanguageCodeActionFn`, etc.) declaring only the members each controller calls; the `OutlineReadPort`/`HoverReadPort`/`CompletionReadPort`/`SignatureReadPort` shapes the UI consumes similarly cannot be imported, so each controller exposes a lazily-built read object (`outlineRead`/`hoverRead`/`completionRead`/`signatureRead` getters) whose shape structurally satisfies them, and `main.ts` relies on the existing blunt `as Parameters<typeof runOpenTuiWorkbench>[2]` cast at the bottom of its UI-options object literal to assert that assignability, exactly as it already did for every other panel. In contrast, the workbench-session methods each controller needs (`activeViewId`/`readView`/`views`/`buffer`/`buffers`/`applyTextEdits`/`beginUndoGroup`/`endUndoGroup`/`applyDocumentEdits`) are declared as one shared narrow port (`LanguageWorkbenchSessionPort`, in `overlays.ts`, reused by `completion.ts`/`workspace-edits.ts`) built directly from workbench's own already-exported `WorkbenchViewSnapshot`/`WorkbenchViewStateSnapshot`/`WorkbenchBufferSnapshot`/`DocumentEdit`/`EditOrigin` types rather than mirrored -- workbench may reference its own sibling modules and the document/contracts packages directly; only `packages/services` types are the ones that must never leak in, matching the ticket's own distinction. `attachNavigation`/`attachLanguage` (one on each of the three controllers) mirror S5/S6's `attachTree`/`attachServices`: they bind the language-service instances main.ts still constructs (in `initializeLanguage`, unchanged) into each controller's private fields, and for overlays/completion also start the model-subscription forwarding to `XI_OUTLINE_STATE`/`XI_HOVER_STATE`/`XI_COMPLETION_STATE`/`XI_SIGNATURE_STATE` markers, returning the `Disposable`(s) for `main.ts` to hold in its own `navigationSubscription`/`completionSubscription`/`signatureSubscription` locals and dispose at the exact original teardown lines -- disposal order for every observable step (`languageSession.dispose()` before `navigationController`/`completionController`/`signatureController`/`workspaceEditCoordinator`, `finishSnippet()`'s replacement `completionFeature.dispose()` staying at the same late `commandRegistry.dispose()`-adjacent position) is unchanged. `renameWithBoundedRetry` and `applyWorkspaceEditProposal` are dynamically-imported free functions from `packages/services/language` that `workspace-edits.ts` cannot call itself (forbidden import, even dynamic -- matches S6/S7's precedent for `applyReplacementEdits`/`matchTaskProblems`), so `main.ts` injects `renameWithRetry`/`runWorkspaceEditProposal` closures that do the dynamic import and call, with `runWorkspaceEditProposal` reproducing the original inline "coordinator/provider/executor/session unavailable" `disposed` guard before importing; `ensureCodeActionExecutor` mirrors the same on-demand-construct-and-memoize pattern as S7's `ensureTaskController` for `executeLanguageCodeAction`. `loadUnopenedWorkspaceFile`/`openWorkspaceEditDocument` moved in full (including their `apply` closures) since every value they touch -- `openTextDocument`/`encodeTextFile`/`positionToOffset` (document package, statically importable), `host.documents`/`host.nextDocumentId()` (workbench's own `BufferHost`), `session.applyDocumentEdits` (the workbench session port) -- is already reachable from workbench; only the platform filesystem calls (`readFile`/`writeFileAtomic`) go through a narrow `WorkspaceEditFilesystemPort` mirroring `SearchFilesystemPort`'s precedent, and `workspaceRelativePathFromUri`/`fileUri`/`workspaceAbsolutePath` are passed as plain curried function values (the first two are services-owned free functions per S6/S7's precedent; the third is `filesystem.workspaceAbsolutePath` bound to `workspaceRoot`). `workspaceOffset`'s `asUtf16Column`/`asLineIndex` position branding cannot use `packages/contracts`'s `asIdentifier`-style validators (contracts re-exports `asLineIndex` but not `asUtf16Column`), so it follows the existing in-repo precedent at `packages/workbench/editing/history-coordinator.ts`'s `checkedUtf16Column`: a manual `Number.isSafeInteger` check followed by an `as` brand cast, imported from `packages/document/src/index` directly (the `LineIndex`/`Utf16Column`/`DocumentSnapshot` types, not primitives) -- functionally identical validation, no forbidden import. The original single `completionOperationNumber` counter (shared by completion/snippet undo-group naming and by `loadUnopenedWorkspaceFile`/`openWorkspaceEditDocument`'s own undo-group naming) is split into one private counter per controller (`CompletionSnippetController`'s and `WorkspaceEditsController`'s own `#operationNumber`); each undo-group id already combines `Date.now()`/a fixed prefix with that counter, so two independent counters can never collide with each other any more than the shared counter could collide with itself across two different call sites -- an internal-uniqueness-only value never asserted or compared across controllers by any test. `planCompletionEdits`/`nonOverlappingDocumentEdits` (pure completion-edit-planning helpers) are exported from `completion.ts` rather than kept private, so `tests/workbench/t116-completion-controller.test.ts` can exercise the overlapping-edit rejection directly instead of only through the full `applyCompletion` flow -- the smallest change that makes the ticket's own suggested test possible without duplicating the logic. `handleSignatureKeypress` (key-driven, forwards the key afterward) and the new `closeSignature()` (plain panel-registry close, no key to forward, matching the original inline `host.registerPanel('signature', { close: () => { signatureOpen = false; signatureController?.cancel(); } })`) are kept as two distinct methods rather than one, since they have different observable effects (one always forwards the triggering key to the active Vim session, the other never does).

S9 judgment calls: every controller S9 depends on that already lives inside `packages/workbench` (`BufferHost`, `WorkbenchSession`, `CommandRegistry`, `ExCommandLineSession`, `PrefixHelpController`, `buildPrefixHelpReadModel`, `OwnedVimSession`/`OwnedVimKeyEvent`, `WorkbenchControlRegistry`, `SplitterDragController`, `WorkbenchPointerCapture`, and the S4-S8 feature controllers themselves) is used *directly*, not mirrored -- only the four S9 controllers' dependencies on each *other* controller are narrow structural ports (`RouterPickerPort`/`RouterExplorerPort`/`RouterSearchPort`/`RouterProblemsPort`/`RouterOverlayPort`/`RouterCompletionPort`/`RouterWorkspaceEditsPort` on `WorkbenchInputRouter`; `PointerPickerPort`/`PointerExplorerPort`/`PointerSearchPort`/`PointerProblemsPort`/`ContextMenuPort` on `WorkbenchPointerRouter`; `HostCommandsWorkspaceEditsPort`/`HostCommandsProblemsPort`/`HostCommandsSaveCoordinatorPort` on `WorkbenchHostCommands`), matching the ticket's own distinction that only `packages/services`/`packages/ui` types must never leak into workbench. `PickerMode`/`WorkbenchPointerEvent`/`WorkbenchPanelPointerEvent`/`ExCommandLineReadPort` (services/ui-owned) are mirrored as `RouterPickerMode`/`PointerWorkbenchEvent`/`PointerPanelEvent`/the router's own `commandLine.read` object, following S5-S8's precedent. `WorkbenchInputRouter` also constructs and owns the `PrefixHelpController`/`ExCommandLineSession` instances itself (previously `main()` locals) since their read-generation callbacks close directly over the router's own `prefixGeneration` counter -- exposing `.prefixHelp` (bound into `runOpenTuiWorkbench`'s `prefixHelp` option) and `.commandLine.read`/`.handleCommandLineKeypress()`/`.isCommandLineActive()` instead. `WorkbenchPointerRouter` similarly constructs and owns `WorkbenchControlRegistry`/`SplitterDragController` itself (previously separate `main()` locals disposed side-by-side); `main.ts` calls `pointerRouter.publishControls([...])` once, after every feature controller referenced by the control `activate` closures exists, exactly where the original `controlRegistry.publish([...])` call sat. `WorkbenchHostCommands`'s `HostNavigationPort` mirrors `HostNavigationController`'s actual `openFile`/`openTag`/`back` methods (not the singular-sounding `file`/`tag` the ticket's own port sketch implied), matching the real services class signature; `resolveHostFilePath`/`openHostLocation`/`handleVimWindowCommand`/`handleWorkbenchCommand` moved verbatim as methods, and `formatCurrentDocument` becomes a one-line delegation to the new `SaveCoordinator.formatView()` (the ticket lists `formatCurrentDocument` under host-commands but its body -- `ensureFormatting`/`formatterPipeline`/`applyConfiguredFormatter` -- under save/checkpoint; splitting the delegation this way keeps both slices' ownership exactly as listed without duplicating the formatter-retry loop). `SaveCoordinator` owns `formatterPipeline`/`formattingInitialization`/`pendingSaves`/`checkpointTimers`/`tornDown` as private fields and exposes `requestSave`/`scheduleCheckpoint`/`formatView`/`dispose()`; `createFormatterPipelineFromEnvironment` (reads `process.env`, dynamically imports `FormatterPipeline`/`createExternalFormatter`) stays a `main.ts` module function exactly as the ticket specifies, injected into `SaveCoordinator` as a `createFormatterPipeline: () => Promise<FormatterPipelinePort | undefined>` factory the coordinator memoizes itself (folding the old separate `formattingInitialization` promise-cache into `ensureFormatting()`). One real ordering bug surfaced and was fixed during this slice (not a "deliberate reorder" like S5's, but a genuine hazard the closure-based original code never had): `BufferHost.createSession(...)`'s initial state publish can synchronously invoke `onPrefixStateChange`/`onCommandLineChange`, and the original code's `schedulePrefixHelp` was a hoisted `function` declaration usable from anywhere in `main()` regardless of textual position, while `inputRouter` is a `let`-bound instance assigned only after every feature controller it depends on exists; calling `host.createSession(...)` at its original position (immediately after `BufferHost` construction) now threw `TypeError: undefined is not an object (evaluating 'inputRouter.schedulePrefixHelp')` (caught by `test:startup`'s packaged-binary PTY run, not `bun run check`/`test:unit`, since neither exercises the actual startup path). Fixed by moving the single `host.createSession(document, id<ViewId>('xi-launch-view'))` call from immediately after `BufferHost` construction to immediately after `pointerRouter.publishControls([...])` -- after `inputRouter`/`hostCommands`/`saveCoordinator`/`pointerRouter` all exist -- with nothing in between reading `host.sessions` synchronously (the only two other `host.sessions.get(...)` call sites are inside `pointerCapture`'s `place`/`scroll` callbacks, invoked only on later pointer events). Also deleted as dead code (unreferenced anywhere, confirmed via full-file grep before removal): the pre-existing `closeAllPanels(keep?: Panel)` function and its `Panel` type alias, both left over from before S5-S8 extracted the panels it would have closed.

The new `ARCH-COMPOSITION-ROOT-01` contract (`tests/architecture/contracts.ts`'s `verifyCompositionRootOwnership`, run by `bun run check:public-boundary`) walks the real AST via `oxc-parser` (already a devDependency, used the same way by `tools/lint/check.ts`) rather than matching source text with regex, per the ticket's explicit requirement. Checks (a)/(b) (no `handle*(Keypress|Pointer|Command)`/`open*`/`close*`-named function-like declaration, no feature-state-shaped `let`) are scoped to descendants of `apps/xi/src/main.ts`'s own top-level `function main(...)`, not the whole file: `main.ts` legitimately keeps module-scope composition helpers the ticket explicitly allows (`openDocument`, `resolveFileArgument`, ...), and a first whole-file version of the check produced false positives on those, and on every `ObjectExpression` `Property` field with an arrow-function value (`openFile: (path) => ...`, `openMatch: (match) => ...`) inside options objects passed into a controller's constructor -- ordinary wiring, not a declared handler. Restricting the function/property-name scan to `MethodDefinition`/`PropertyDefinition` (class members) plus real `FunctionDeclaration`s and named `const`/`let` function bindings, and restricting *where* it looks to inside `main()`'s own body, resolved both without an allowlist. Check (c) (no `main.ts` function declaration over 120 lines except `main` itself) scans the whole file, since it is about function size everywhere in the composition root, not closure-state placement. Verified failing first against a reproduction of the pre-S9 shape (the exact removed names/signatures -- `handleWorkbenchControl`, `handlePanelPointer`, `openPanelContextMenu`, `cancelSplitterDrag`, `handleLeaderKeypress`, `handleCommandLineKeypress`, `handleVimHostCommand`, `handleWorkbenchCommand`, and the `leaderPending`/`leaderPanelPending`/`macroRegisterPending`/`prefixGeneration` lets, all nested inside a `function main()` -- reproducing the *shape* T116 S4-S9's own evidence and this session's direct reads of the pre-S9 file establish, since a literal `git` revert was out of scope for a structural-refactor session with many unrelated uncommitted changes already in the tree and no permission to touch git state): 10 violations, one per removed name, confirmed before the same check was run against the real repository and passed with zero failures.

## Executed validation

| Command / fixture | Actual result | Evidence path / retained CI artifact |
|---|---|---|
| Baseline `bun run check` (before S1) | pass | — |
| Baseline `bun run test:unit` | pass, 36 fixtures | job tmp baseline/unit.log |
| Baseline `bun run test:ui` | pass | job tmp baseline/ui.log |
| S1 `bun run check` | pass | — |
| S5 `bun run check` | pass | — |
| S5 `bun run test:unit` | pass, 39 fixtures (new: `tests/workbench/t116-explorer-controller.test.ts`) | — |
| S5 `bun test tests/e2e/t040-explorer.test.ts` | pass | — |
| S5 `python3 tests/e2e/t045-e03-explorer-filter-reveal-pty.py` | pass | — |
| S5 `python3 tests/e2e/t131-explorer-file-management-pty.py` | pass | — |
| S5 `python3 tests/e2e/t045-keyboard-only-journey-pty.py` | pass | — |
| S5 `python3 tests/distribution/t064-editor-pty.py` | pass | — |
| S5 `bun run test:startup` | pass | — |
| S6 `bun run check` | pass | — |
| S6 `bun run test:unit` | pass, 40 fixtures (new: `tests/workbench/t116-search-controller.test.ts`) | — |
| S6 `python3 tests/e2e/t043-search-pty.py` | pass | — |
| S6 `python3 tests/e2e/t044-replace-pty.py` | pass | — |
| S6 `python3 tests/e2e/t045-e05-search-rapid-typing-pty.py` | pass | — |
| S6 `python3 tests/e2e/t045-e06-replace-dirty-closed-pty.py` | pass | — |
| S6 `python3 tests/e2e/t045-e15-degraded-missing-rg-pty.py` | pass | — |
| S6 `python3 tests/e2e/t129-panel-scroll-picker-search-pty.py` | pass | — |
| S6 `python3 tests/e2e/t045-keyboard-only-journey-pty.py` | pass | — |
| S6 `python3 tests/distribution/t064-editor-pty.py` | pass | — |
| S6 `bun run test:startup` | pass | — |
| S7 `bun run check` | pass | — |
| S7 `bun run test:unit` | pass, 41 fixtures (new: `tests/workbench/t116-problems-controller.test.ts`) | — |
| S7 `python3 tests/e2e/t049-problems-pty.py` | pass | — |
| S7 `python3 tests/e2e/t060-tasks-pty.py` | pass | — |
| S7 `python3 tests/e2e/t127-problems-live-diagnostics-pty.py` | pass | — |
| S7 `python3 tests/e2e/t045-keyboard-only-journey-pty.py` | pass | — |
| S7 `python3 tests/distribution/t064-editor-pty.py` | pass | — |
| S7 `bun test tests/e2e/t045-g3.test.ts` | pass | — |
| S7 `bun run test:startup` | pass | — |
| S8 `bun run check` (after each sub-step: overlays, workspace edits, completion/signature/snippet; and at the end) | pass | — |
| S8 `bun run test:unit` | pass, 44 fixtures (new: `tests/workbench/t116-language-overlays.test.ts`, `tests/workbench/t116-completion-controller.test.ts`, `tests/workbench/t116-workspace-edits.test.ts`) | — |
| S8 `python3 tests/e2e/t050-outline-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t051-completion-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t052-workspace-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t071-native-navigation-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t088-completion-failures-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t088-multi-snippet-pty.py` (live `typescript-language-server`) | pass | — |
| S8 `python3 tests/e2e/t045-e16-narrow-popup-resize-pty.py` | pass | — |
| S8 `python3 tests/e2e/t055-formatting-pty.py` | pass | — |
| S8 `python3 tests/e2e/t045-keyboard-only-journey-pty.py` | pass | — |
| S8 `python3 tests/distribution/t064-editor-pty.py` | pass | — |
| S8 `bun test tests/e2e/t069-completion.test.ts` | pass | — |
| S8 `bun test tests/e2e/t071-native-navigation.test.ts` | pass | — |
| S8 `bun test tests/e2e/t056-g4.test.ts` | pass | — |
| S8 `bun run test:startup` | pass | — |
| S9 `bun run check` (incl. new `ARCH-COMPOSITION-ROOT-01`) | pass | — |
| S9 `bun run test:unit` | pass, 48 fixtures (new: `tests/workbench/t116-input-router.test.ts`, `tests/workbench/t116-pointer-router.test.ts`, `tests/workbench/t116-host-commands.test.ts`, `tests/workbench/t116-save-coordinator.test.ts`) | — |
| S9 pre-move `bun run tests/architecture/contracts.ts` against a reproduction of the pre-S9 `main()` shape | fails with 10 violations (one per removed handler-shaped name/feature-state `let`) | job tmp s9-arch-pre-move.log |
| S9 post-move `bun run tests/architecture/contracts.ts` | passes, reports `ARCH-COMPOSITION-ROOT-01` alongside the existing 8 contracts | — |
| S9 `python3 tests/distribution/t064-editor-pty.py` | pass | — |
| S9 `python3 tests/distribution/t064-vim-pty.py` | pass | — |
| S9 `python3 tests/distribution/t064-ex-pty.py` | pass | — |
| S9 `python3 tests/distribution/t064-file-pty.py` | pass | — |
| S9 `python3 tests/e2e/t045-e02-write-quit-all-pty.py` | pass | — |
| S9 `python3 tests/e2e/t045-e11-resize-sequence-pty.py` | pass | — |
| S9 `python3 tests/e2e/t045-e12-bracketed-paste-pty.py` | pass | — |
| S9 `python3 tests/e2e/t045-e13-crash-recovery-pty.py` | pass | — |
| S9 `python3 tests/e2e/t055-formatting-pty.py` | pass | — |
| S9 `python3 tests/e2e/t127-panel-pointer-pty.py` | pass | — |
| S9 `python3 tests/e2e/t127-panel-pointer-picker-pty.py` | pass | — |
| S9 `python3 tests/e2e/t128-e22-splitter-xterm-pty.py` (live `xterm`) | pass | — |
| S9 `python3 tests/e2e/t129-context-menu-pty.py` | pass | — |
| S9 `python3 tests/e2e/t129-mouse-toggle-pty.py` | pass | — |
| S9 `python3 tests/e2e/t130-dot-repeat-pty.py` | pass | — |
| S9 `python3 tests/e2e/t130-macro-pty.py` | pass | — |
| S9 `python3 tests/e2e/t130-change-repeat-pty.py` | pass | — |
| S9 `python3 tests/e2e/t045-keyboard-only-journey-pty.py` | pass | — |
| S9 `python3 tests/e2e/t071-native-navigation-pty.py` (live `typescript-language-server`) | pass | — |
| S9 `bun run tests/e2e/t038-session.test.ts` | pass | — |
| S9 `bun test tests/e2e/t045-g3.test.ts` | pass | — |
| S9 `bun run test:startup` | pass (caught and fixed the `host.createSession`/`inputRouter` ordering bug -- see judgment calls) | — |
| S9 skipped: `tests/e2e/t039-picker-pty.py` (known pre-existing intermittent hang, per assignment) | not run | — |
| S10 `bun run check` | pass (9 architecture contracts incl. `ARCH-COMPOSITION-ROOT-01`) | job tmp final/check.log |
| S10 `bun run test:unit` | pass, 48 fixtures | job tmp final/unit.log |
| S10 `bun run test:ui` | pass, 9 fixtures | job tmp final/ui.log |
| S10 `bun run test:services` | pass, 34 fixtures | job tmp final/services.log |
| S10 `bun run test:e2e` (run 1) | failed at `tests/distribution/t064-ex-pty.py` ("did not shut down"); rerun alone passes | job tmp final/e2e.log |
| S10 `tests/distribution/t064-ex-pty.py` ×10 refactored tree | 8/10 pass; 2 × "dirty :q did not report the unsaved-change refusal" | job tmp final/ex-loop-current.log |
| S10 `tests/distribution/t064-ex-pty.py` ×10 pre-refactor snapshot (615bf9c + pre-existing untracked files) | 9/10 pass; 1 × identical "dirty :q" failure | job tmp final/ex-loop-baseline.log |
| S10 `bun run test:e2e` (run 2, full) | pass, 68 fixtures | job tmp final/e2e-2.log |
| S10 `bun run test:startup` | pass (compiled binary, renderer entry, startup input) | job tmp final/startup.log |
| S10 `bun run bench -- --suite interaction --baseline revision` | tooling error: "baseline/candidate must resolve to exactly one current-catalog run" (no ledger run for `revision`; T106 gap, pre-existing) | job tmp final/bench.log |
| S10 `bun run bench -- --suite interaction` | failed at `bench/t043-search.bench.ts` warm first-result p95 budget (100 ms); bench code and `packages/services`/`packages/platform` are byte-identical in both trees; isolated reruns below | job tmp final/bench-current.log |
| S10 `bench/t043-search.bench.ts` isolated, alternating | refactored: 102.8 / 106.0 / 104.7 ms p95; snapshot: 87.9 / 105.3 / 115.7 ms p95 → host noise, budget borderline on this host for both trees | job tmp final/t043-*.log |
| S10 pre-S9 contract probe | `ARCH-COMPOSITION-ROOT-01` fails against the real pre-S9 `main.ts` (snapshot 2070494) copied into `apps/xi/src/` (handlePanelPointer, openPanelContextMenu, handleLeaderKeypress, …, `let leaderPending` …); passes on the refactored tree | — |

## Performance and visual evidence

Paired diagnostic probes (`bench/performance/t116-startup.py`, `bench/performance/t116-key-output.py`; classification `diagnostic`, 30 trials each, `bun run apps/xi/src/main.ts` source mode, same host/terminal/PTY adapter) on the refactored tree ("cur") and the pre-refactor snapshot ("base", `git stash create` 615bf9c plus the pre-existing untracked files), alternating A/B:

| Probe | Run | cur p50 | cur p95 | cur max | base p50 | base p95 | base max |
|---|---|---|---|---|---|---|---|
| startup wall ms | 1 | 127.8 | 141.1 | 152.6 | 123.0 | 133.5 | 145.7 |
| startup wall ms | 2 | 128.5 | 141.9 | 144.5 | 125.3 | 153.9 | 157.0 |
| startup wall ms | 3 | 132.4 | 159.1 | 183.7 | 121.8 | 134.1 | 137.9 |
| startup child cpu ms | 1–3 | 151–157 | 165–189 | — | 145–149 | 156–183 | — |
| key→first output ms | 1 | 1.09 | 4.08 | 15.8 | 0.70 | 3.32 | 7.9 |
| key→first output ms | 2 | 1.28 | 6.55 | 7.6 | 0.60 | 3.55 | 8.6 |
| key→first output ms | 3 | 0.49 | 3.06 | 8.8 | 1.70 | 7.92 | 13.1 |

Reading: key-output p50/p95 overlap across the three pairs in both directions (no regression signal). Warm startup p50 is consistently 5–10 ms (≈4–8 %) higher on the refactored tree across all three pairs in source-run mode, with p95 overlapping (both trees have one pair above the 150 ms STARTUP-WARM p95 target on this shared host). The refactor adds 21 workbench source modules on the launch import path; in `bun run` source mode each is a separate transpile/evaluate unit, which is the most plausible cause. The compiled-binary path (`bun run test:startup`) passes. This probe is diagnostic by its own classification and does not qualify STARTUP-WARM either way; the p50 increase is recorded as an open item, not dismissed. Raw artifacts: `.artifacts/performance/T116/startup-run-s10{,-2,-3}.json`, `key-output-run{,-s10-2,-s10-3}.json`; snapshot-side files in the job tmp baseline tree.

Layout split (S2): `bench/layout/t014-viewport.ts` cacheStats byte-identical before/after; p95 within run-to-run noise (job tmp s2-bench-before/after.txt). No visual changes were made; PTY journeys cover rendering paths.

## Failure cases and recovery

Each slice is preceded by a `git stash create` snapshot recorded here so a failed slice can be restored with `git checkout <sha> -- .` without touching pre-existing work.

## Limitations and next action

- T116 status stays `todo` in tickets.json with this report linked: `tools/plan.py check` rejects `in_progress`/`done` while dependency T106 is unfinished. Structural acceptance (thin CLI, owned orchestration, import/ownership check, real PTY journeys, compiled startup path) is complete; the STARTUP-WARM/STARTUP-COLD *qualification* rows still need the T106/T115 production producers.
- Open item: warm-startup p50 +5–10 ms in source-run mode (table above). Next action: measure the compiled binary with the same 30-trial protocol and, if it reproduces, batch the workbench controller modules behind the existing `packages/workbench/src/entrypoints/launch` lazy import boundary.
- Pre-existing flakes, reproduced on the pre-refactor snapshot, not fixed here: `tests/e2e/t039-picker-pty.py` intermittent hang (orphaned async preview open after cancel); `tests/distribution/t064-ex-pty.py` "dirty :q refusal not seen" / "did not shut down" roughly 1–2 in 10 (250 ms drain window). Both need a generation guard / longer read window as separate behavior tickets.
- `bun run bench -- --baseline revision` cannot resolve a baseline run (T106 ledger gap); `bench/t043-search.bench.ts` 100 ms first-result budget is borderline on this host for both trees.
- Left in place deliberately: composition/adapter helpers in `main.ts` (`openDocument`, explorer filesystem adapters, theme discovery, formatter-from-environment) and lazy service construction; `createOwnedVimSession` remains one 1 459-line closure (S3 split only its helpers).
