# T060 — Implement task output and diagnostics

## Current status — reopened 2026-09-15

**Unproven / todo.** The report says no real child process is started by its fixture and defers problem matchers/E17. Reaping and loaded typing budgets remain unproven.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T060 — Implement task output and diagnostics
- Outcome: passed for bounded task process/output owner
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T060-source-manifest.sha256](T060-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2; OpenTUI 0.5.11
- Specifications: `docs/plan/04-services.md` task process contract and `docs/plan/05-validation.md` E17

## Observable result

`TaskController` launches through an injected process factory, retains bounded stdout/stderr, reports truncation and exit status, and disposes subscriptions/processes on cancel. `TaskOutputRenderable` presents a clipped read model; task output does not overwrite LSP diagnostics.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/tasks/t060-tasks.test.ts` | Passed bounded output retention, truncation, exit status and cancellation/disposal lifecycle. | `tests/tasks/t060-tasks.test.ts`; fixtures `T060-OUTPUT-01`, `T060-EXIT-01` |
| `bun x tsc --noEmit` | Passed strict repository typecheck. | Command output |
| `bun run check:public-boundary` | Passed. | Command output |
| `git diff --check` | Passed. | Command output |

## Performance and visual evidence

Output is capped at the configured byte budget and the UI clips to visible rows, protecting typing from output floods. No real child process is started by the fixture; platform process integration remains a host concern.

## Failure cases and recovery

Spawn/exit failures remain typed. Cancellation terminates the owned process and disposes listeners. Excess output is retained only up to the bound with an explicit truncated flag.

## Limitations and next action

Problem matcher integration and full E17 PTY process-reaping qualification remain part of T065/T066.
