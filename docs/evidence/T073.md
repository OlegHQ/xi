# T073 — Multi-root and platform qualification

## Current status — reopened 2026-09-15

**Unproven / todo.** Completion is withdrawn because prerequisite acceptance was reopened: T042, T044, T057. Existing component evidence is retained; this is not a newly observed defect in this ticket's implementation.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T073 — Multi-root and platform qualification
- Outcome: passed for root identity and platform-owned service boundaries
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T073-source-manifest.sha256](T073-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2; OpenTUI 0.5.11
- Specifications: `docs/plan/01-architecture.md`, `docs/plan/04-services.md`, `docs/plan/06-execution.md`

## Observable result

Repeated relative paths remain distinct across workspace roots in the file index and search service. Language server identity keys include the resolved root, so sessions for the same workspace and server configuration cannot collide across roots. Root removal only removes that root's entries.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/workspace/t073-multi-root.test.ts` | Passed duplicate relative-path indexing, root-scoped search, root removal isolation and language-session identity checks. | `tests/workspace/t073-multi-root.test.ts`; fixtures `T073-MULTIROOT-01` through `T073-MULTIROOT-04` |
| `bun x tsc --noEmit` | Passed. | Command output |
| `bun run check:public-boundary` | Passed public DAG and architecture sentinels. | Command output |

## Performance and visual evidence

The qualification exercises bounded in-memory models and root identity. Filesystem watcher, process and terminal platform behavior remains covered by their owning tickets and release gates.

## Failure cases and recovery

Duplicate root IDs are rejected, unknown roots cannot receive paths, root removal is isolated, and search never returns disk matches from another root. Session keys differ for distinct roots.

## Limitations and next action

This ticket does not certify every operating-system backend; platform ports remain injected and are validated by their focused fixtures.
