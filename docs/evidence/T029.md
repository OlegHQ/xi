# T029: Coordinate structural motions and remaining operator parity

- Ticket ID and title: T029 — Coordinate structural motions and remaining operator parity.
- Outcome: passed for the scope-preserving split. T102–T105 and the T032 direct-change slice provide the required structural and operator evidence; provider boundaries and the exhaustive help audit remain explicit downstream work.
- Implementation revision or tree hash: shared working tree, uncommitted.
- Environment and pinned dependency/oracle versions: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2; OpenTUI 0.5.11; pinned Neovim 0.12.4 with binary/runtime hashes recorded in `tests/oracle/manifest.json`.
- Specification sections and acceptance items covered: `docs/plan/01-architecture.md`, `docs/plan/02-vim.md`, `docs/plan/05-validation.md`; structural motions, direct changes, text transforms, advanced operators, and qualification evidence.

## Observable result

The original broad T029 work is split into independently evidenced families without dropping its acceptance surface. T102 covers bounded structural motions and option-dependent nesting; T103 covers case, join, indent and format operators; T104 covers filter, fold and numeric operators; T105 qualifies their 38 pinned fixtures and failure rows. T032 adds the remaining direct `s`, `S`, `C` and `r` plans, while T022 owns replace-mode `R`/`gR` and T103 owns `J`/`gJ` and related transforms.

All Xi paths remain pure planner/transaction owners: they read immutable versioned document snapshots, return typed failures and provider requests, and leave publication to the document/workbench coordinator. No Neovim process or fallback exists in production code.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/vim/motions/t102-structural.test.ts` | Passed 14 pinned structural fixtures and unmatched, stale, count and scan-limit failures. | [T102.md](T102.md) |
| `bun run tests/vim/operators/t103-text-transform.test.ts` | Passed case, join, indent, provider, cancellation, malformed-text and stale-version cases. | [T103.md](T103.md) |
| `bun run tests/vim/operators/t103-oracle.test.ts` | Passed 12 pinned Neovim text-transform fixtures. | [T103.md](T103.md) |
| `bun run tests/vim/operators/t104-advanced.test.ts` | Passed filter, fold, output-limit, cancellation and numeric `nrformats` cases. | [T104.md](T104.md) |
| `bun run tests/vim/operators/t104-oracle.test.ts` | Passed pinned decimal and hexadecimal Neovim numeric fixtures. | [T104.md](T104.md) |
| `bun run tests/vim/config/t032-config.test.ts` | Passed profile mapping/option fixtures and pinned mapping/option rows. | [T032.md](T032.md) |
| `bun run tests/vim/config/t032-direct-changes.test.ts` | Passed the four remaining direct-change rows and four pinned Neovim direct-change rows. | `tests/vim/config/t032-direct-changes.test.ts` |
| `bun run check` | Passed toolchain, strict TypeScript, public-boundary and architecture checks. | Current command output |
| `python3 tools/plan.py check` | Passed the 105-ticket dependency graph and report references. | Current command output |
| `sha256sum -c docs/evidence/T102-source-manifest.sha256` through `T105-source-manifest.sha256` | Passed all listed child evidence inputs after the shared Vim barrel update. | Child source manifests |

## Performance and visual evidence

No UI or aggregate latency gate applies to this coordination ticket. The child reports record bounded scans, cancellation and provider/output limits; release performance remains a later gate.

## Failure cases and recovery

Child evidence retains typed unmatched-structure, stale-version, invalid-range, provider-unavailable, cancellation, output-limit, malformed-text, numeric-overflow and invalid direct-change failures. The qualification fails closed when a child manifest or report is missing.

## Limitations and next action

The generated 597-row pinned-help inventory remains provisional until T061 completes its exhaustive audit. Provider-backed filter, fold, indent and format operations require injected Xi providers. These limitations are documented boundaries, not omitted operator families. T029 is complete; G2 and the first editor UI still require the downstream multi-selection and workbench tickets.
