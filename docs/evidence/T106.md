# T106 — Performance evidence foundation (in progress)

- Ticket: T106, Enforce performance catalog, provenance and real baseline comparison.
- Outcome: **unproven / in progress**. This report covers an implemented comparator and suite-discovery slice, not completion of T106 or editor performance qualification.
- Base revision: `555ed0ce3c2475a10844824468e6fddbb6e49755`, with substantial pre-existing uncommitted implementation. No commits/pushes were made in this slice.
- Earlier source file manifest: `.artifacts/performance/T106/source-manifest.json`. The final PF12 collector records and checks the exact six-file source identity in the retained artifact before every trial and after collection.
- Environment: Linux 6.8.0-139-generic, aarch64, glibc 2.42; shared VM, **diagnostic**, no certified reference host or terminal. Bun 1.3.13, TypeScript 7.0.2, OpenTUI 0.5.11, Python 3.13.15 and SQLite 3.51.2. No Vim semantics changed; no oracle run required for this tool slice.
- Specifications: 01 architecture (development-only SQLite), 05 benchmark method/required commands, 12 SQLite ledger/PF12, 13 aggregate baseline-discovery defect.

## Observable result

`tools/perf.py resolve` now resolves an immutable run ID, unique full source-manifest SHA-256, or unique full recorded Git revision. Missing, stale, ambiguous and self baselines cannot compare. Revision indexes are derived, additive SQLite tables; imports remain atomic and immutable.

The adapter catalog now declares 53 deterministic workload variants across
PF01–PF12 and expands the 55 budget adapters into 1,016 unique
workload/metric/statistic cells. Every cell records its generator selector,
correctness assertion, cold/warm state set, producer boundary, metric source and
sample policy. Observation rows carry the variant ID, and the ledger retains
legacy rows under an ineligible marker instead of counting them toward current
coverage. Declared cells are still distinct from runnable, observed and
qualified cells.

`tools/perf.py status` now reports declared, runnable, observed and
reference-classified counts separately. The current catalog reports 1,016
declared cells, 0 runnable producers, 0 observations and 0
reference-classified cells. Reference-classified rows still do not release
qualify the matrix without complete coverage and host/protocol gates.

`compare BASELINE CANDIDATE [BUDGET-ID ...]` rechecks retained artifact hashes, manifest identities, exact corpus/environment/protocol compatibility and raw metric coverage. It recomputes imported statistics from finite samples, matches units/parameters, checks both runs' minimum sample counts and rejects summary-only evidence. Selected numeric comparisons use actual baseline distributions. Candidate absolute failures or a significant >10% p95 regression fail even inside the other absolute targets. A CI crossing the threshold is unproven. The independent-trial bootstrap uses 2,000 resamples, seed 41027; session-correlated typing and derived comparison metrics require further adapters.

Every comparison explicitly reports `release: unproven`. Source-manifest hashes bind supplied manifests; they do not authenticate production measurement or a dedicated host. Full production provenance, host certification and coverage remain unfinished. See [artifact format and command documentation](../../bench/performance/README.md).

`tools/verify-suite.ts` now rejects missing/repeated option values, missing explicit fixture files, empty required roots, invalid manifests and unregistered benchmark selectors. `bench/manifest.json` lists actual executable benchmarks so diagnostic helpers requiring arguments are not accidentally run by aggregate discovery. The unit suite now runs five previously omitted standalone document scripts (`verify`, T010, T011, T012, T101), and the Python performance regression suites. Every required document script is checked for existence before any fixture executes.

Reference classification now has an explicit provenance guard: a ledger run must
carry a dedicated host ID, qualification method and SHA-256 qualification evidence
identity, and comparison revalidates that the raw artifact carries the same host
metadata. Missing, shared-host or mismatched metadata is rejected. This is a
schema/integrity guard; the current shared VM remains diagnostic and no host has
been qualified by this worktree.

The pinned-runtime sampler is now shared by diagnostic probes in
`bench/performance/runtime-accounting.ts`. It records CPU and wall timing,
signed process/JSC retention deltas, current and peak native commit, process
RSS, and caller-supplied worker attribution. JSC `extraMemorySize` remains a
separate inclusive field; wrapper and total-allocation counters explicitly
remain unsupported on Bun 1.3.13. `tests/performance/t106-runtime-accounting.test.ts`
checks these distinctions and the negative-delta path.

The sampler source hash for this tree is
`73ffec5a37ad3a0b6085da295e196b4a674764576668ea7dc232979d2d4d6153`.

The aggregate `--baseline` option now resolves real raw baseline evidence and explicitly fails on the remaining missing invocation-bound candidate producer. It no longer logs an arbitrary label and reports success. Connecting all production producers remains T106/T115 work, not a suppressed gate.

## Executed validation

| Command / fixture | Actual result | Evidence |
|---|---|---|
| `python3 -m unittest discover -s tests/performance -p 'test_*.py'` | 36 tests passed: 15 comparison, 12 ledger, 7 suite CLI/discovery, and 2 deterministic workload-generator tests | command output |
| `bun run test:unit` | Passed 23 executable fixtures, including all 34 Python performance tests, document checks, and the runtime accounting contract fixture. | command output |
| `bun run check` | Pinned toolchain, strict TypeScript and public/import ownership checks passed | command output |
| `bun run verify:release` | Earlier unit/Vim/services/UI/e2e stages passed; the final benchmark stage correctly stopped with `baseline/candidate must resolve to exactly one current-catalog run` because no qualified paired runs exist. | command output |
| `python3 tools/perf.py check` | 55 budgets and 1,016 declared workload/metric obligations valid; no measurement certification | Catalog hash below |
| `bun test --timeout 30000 tests/performance/t106-runtime-accounting.test.ts` | Passed 2 runtime accounting contract tests; total allocation remains explicitly unsupported. | `bench/performance/runtime-accounting.ts` |
| `python3 tools/plan.py check` | 121-ticket dependency graph and report references valid | Ticket remains in progress |
| `python3 bench/performance/ledger-trials.py .artifacts/performance/T106/ledger-trials-reference-host-guard.json` | 30 independent fresh processes, each importing 100,000 variant-bound synthetic rows and checking 1,000 indexed queries; source identity remained unchanged | `.artifacts/performance/T106/ledger-trials-reference-host-guard.json` |

Many existing `.test.ts` files use top-level assertions and print named checks, so Bun reports zero registered `test()` calls for those scripts. The report counts executable fixture files, not fictitious Bun test cases. Their assertion output and exit status are retained; the Python suites report actual unittest counts.

## Performance evidence

PF12 / `LEDGER`, development control path only. Catalog hash:
`1129c2799ffa38d6980cee5ad84d1f9bca9b16307f1ae3ce396f14b2fa25aa51`.
The source/corpus for these measurements is the synthetic ledger workload, **not editor text operations**. No Vim/UI CPU or memory improvement is inferred.

| Measurement across 30 fresh processes | p50 | p95 | p99 / max |
|---|---:|---:|---:|
| 100k-row import wall ms | 1333.192 | 1395.964 | 1472.636 |
| 100k-row import CPU ms (user + system) | 1208.068 | 1255.630 | 1366.423 |
| Each process's 1,000-query p95, ms | 0.008250 | 0.008875 | 0.009375 |
| Process high-water RSS, KiB | 28332 | 28416 | 28420 |

The query row is a distribution of **per-process p95 values**, not a pooled query percentile. Each raw trial also retains its query p50/p95/p99/max and SQLite query plan. Maximum RSS is 27.75 MiB. The numeric values are below PF12 targets, but this shared-host diagnostic is **not a reference-host pass**. No compatible baseline pair, randomized ordering, allocation census or native/editor/worker attribution was collected by this ledger-only run. CPU measurements include bundle serialization/read/import in the child; query timings are separate.

An earlier 30-trial diagnostic is preserved as `.artifacts/performance/T106/ledger-trials.json`. It lacks the collector's source-invariance proof and is not used for qualification. The identified artifact SHA-256 is `73b1b8fc0c753472504f247e3c64d44f30dd8e21759c52a80ffe5207a5c132ab`. Large traces remain ignored; the small sanitized summary is retained in this repository.

The final variant-bound artifact SHA-256 is
`eaa18d815f2fa2906f77379a9831049da5aad63082270776e2f71c456a1672a5`; its
source-manifest hash is
`2a02fec5f7fb4c94d7b802aea0934dbd704d2fcb5f529a2c4c306c9aa556edc1`.

Final source hashes included by that manifest:

```text
990c9c028a12c40073f8eaae5e304b21aa9e058d61a5a99525f8c0130cce5bec  tools/perf.py
4470e6b342b66ec7981fc42f934f63125a8c1ed53c85a1f77c0b85a679f66d8a  bench/performance/ledger-probe.py
8db316fc79403eef1c790b45295f86ef991d4c1999623e8adc98f41679aeddf8  bench/performance/ledger-trials.py
ac093a22c6fbc499ac97524444fd8bde1a659f9d82a7b4f0de3c016783f780c6  bench/performance/workloads.py
7a1b88c649c849673747e14213863a7931371bebf30dc02ca9c77ce00e9087e5  docs/plan/performance-adapters.json
```

No visual changes; screenshot/PTY visual qualification does not apply to this tooling slice.

## Failure cases and recovery

Tests demonstrate: transactional rollback after a later invalid bundle, immutable run IDs, parameterized bounds, strict less-than, signed regressions, SQL parameter binding/index use, stale catalog and artifact hashes, missing/deleted/tampered artifacts, malformed/nonfinite measurements, duplicate JSON keys, wrong units, missing samples, raw/summary disagreement, mismatch of source manifests/corpus/environment/protocol, correlated-sampling rejection, significant regressions, uncertain CIs, exact 10% floating-point boundary, unknown/empty selection, missing or ambiguous full revision, and standalone fixture deletion before execution.

The suite reproduction uses disposable roots; no personal workspace contents are copied to the ledger. Existing planning/diagnostic bundles remain importable as numeric observations and cannot masquerade as raw comparison evidence. Database loss remains recoverable from catalog and retained bundles.

## Limitations and next action

T106 is **not done**. Outstanding original acceptance:

1. The declaration slice now defines 1,016 variant/metric/statistic obligations for all 55 operations and validates missing or duplicate cells. Actual production producers and fixture/environment/source coverage against their manifests remain unimplemented; the current status is declared 1,016, runnable 0, observed 0, reference-classified 0.
2. The shared pinned-runtime sampler now covers CPU/wall timing, signed process/JSC deltas, native commit, RSS and caller-supplied worker attribution. Production adapters still need to bind those counters to every operation and provide document work counters. Total allocation and document wrapper counters remain unsupported; this slice adds no claim of zero allocation.
3. Add complete semantic PF corpora and hash-checked producer binding. The catalog has deterministic variant generator declarations and a materializer; the current materializer is not a substitute for complete production corpus fixtures, and PF04 still requires the pinned SQLite artifact.
4. Add session/block confidence analysis and collection-order evidence for the required 10,000 interactive samples, production reference-host qualification, and derived Neovim/regression metrics.
5. Attach actual invocation-bound producers to aggregate suite selectors. `--baseline` intentionally remains failing until measurements can support its claimed comparison. Final complete integration remains T115.
6. Qualify PF12 on a documented reference host; these 30 diagnostic trials do not satisfy that host requirement.

Resume with `python3 tools/plan.py show T106` and the source/README above. `next` may list no todo ticket while T106 is in progress; this is not an external blocker and no prerequisite may be marked done to unlock its dependents. The full editor implementation, parity and release goals remain active.

## 2026-09-17 update: first real observations imported; T106/T115 ownership boundary re-verified

**Outcome: still unproven / in progress — but no longer "observed 0".** This update did two things: (1) re-verified the existing tooling layer is sound (36 pre-existing tests still pass, plus 3 new ones), correcting a stale limitation in this report; (2) imported the first real, honest observations this ledger has ever held.

### Re-verification (no code change)

- Item 3 above ("PF04 still requires the pinned SQLite artifact") is **stale**: `.artifacts/performance-planning/sqlite3.c` exists and its SHA-256 (`e3f5d6901e7492af4a1fc8c4d745cae84c264942524c3fbfc02b82a5ca8818c8`) matches `bench/performance/workloads.py`'s pinned `SQLITE_SHA256`. `python3 bench/performance/workloads.py PF04-<variant> <out>` materializes correctly. This item is done; it was not re-checked before this update.
- Re-read `docs/plan/14-remediation-handoff.md`'s ownership table against `docs/plan/performance-adapters.json`: **every one of the 55 `production_adapters` rows, including the `LEDGER` row whose `owner` is `performance` (T106 itself, not another team), declares `producer.selector: "pending:T115:adapter.<id>"`.** This is a deliberate, uniform convention, not an oversight — it confirms "T115 wires and qualifies all production cells" applies to literally every budget, T106's own dogfood metric included. Wiring a `pending:` selector to a real value is T115's scope; this update does not do that, and does not change any adapter's `producer.selector`.
- `python3 -m unittest discover -s tests/performance -p 'test_*.py'`: 39/39 pass (36 pre-existing + 3 new, below).
- `bun run check`, `bun run test:unit`, `python3 tools/plan.py check`, `python3 tools/perf.py check`: all pass.

### First real observations

`bench/performance/ledger-trials.py` (T106's own pre-existing tool) was run fresh for a real 30-trial PF12 collection — real subprocesses, real SQLite import/query timing, real RSS, on this shared diagnostic VM (`classification: "diagnostic"`, `reference_host: false`; a dedicated, certified reference host does not exist in this environment and none was fabricated — `tools/perf.py`'s `qualified_reference_host()` requires `dedicated: true`, which this VM is not). Result: `.artifacts/performance/T106/ledger-trials-2026-09-17.json`.

A new tool, `tools/import-t106-ledger-pf12.py`, converts that report into a `validate_bundle`-compliant observation bundle and imports it via the existing `tools/perf.py` machinery — no changes to `tools/perf.py` itself. It deliberately covers **only** what the 30 trials actually measured:

| Budget | Variant | Metric:statistic | Value | Bound | Assessment |
|---|---|---|---|---|---|
| LEDGER | PF12-100k-import | import:p95 | 1565.00 ms | ≤ 5000 ms | within-target-diagnostic |
| LEDGER | PF12-100k-import | rss_peak:max | 29,151,232 bytes | ≤ 100,663,296 bytes | within-target-diagnostic |
| LEDGER | PF12-1k-indexed-query | lookup:p95 | 0.0096 ms | ≤ 10 ms | within-target-diagnostic |

`PF12-malformed-evidence` and `PF12-stale-evidence` (the two other declared variants for this budget, covering rejection-path timing) are deliberately **not** touched — this trial run does not measure them, and the importer refuses to claim coverage it does not have. `python3 tools/perf.py status` now reports `observed 3` (was 0) out of 1,016 declared cells; `runnable` correctly stays 0 (no `producer.selector` was changed). Retained artifacts: `.artifacts/performance/T106/ledger-trials-2026-09-17.json` (raw 30-trial report), `.artifacts/performance/T106/ledger-trials-2026-09-17-bundle.json` (the imported bundle), `.artifacts/performance/T106/corpus/PF12-100k-import.bin` (the materialized, hash-verified corpus backing `corpus_hash`).

`tests/performance/test_ledger_pf12_import.py` (3 tests, all passing) checks the bundle-construction logic in isolation: measured metrics map to their specific variants (not smeared across all four), the rejection-path variants never appear, a `classification: "reference"` or a report with fewer than 30 trials is rejected before touching the ledger, and a built bundle imports cleanly into a fresh ledger with the expected `within-target-diagnostic` assessments.

### What this does and does not prove

This does **not** move T106 toward done. `declared 1016; runnable 0; observed 3` is still overwhelmingly unproven, and 3 diagnostic-classified cells for one dogfood budget do not qualify PF12 (a certified reference host is still required for that, per this ticket's own acceptance item 1 and limitation item 6 above — genuinely absent from this environment, not a shortcut). It does prove: the full obligation → bundle → validation → import → status pipeline this ticket built actually works end-to-end against a real measurement, for the first time since this catalog was declared. That is a meaningfully different, verified claim than "the schema validates" (which the pre-existing 36 tests already covered) — it is "a real number went all the way through and landed correctly classified."

### Second real observation: RETENTION

The suggested next action above was carried out in the same session. `bench/performance/t114-workbench-resources.ts` (a pre-existing single-run diagnostic exercising `WorkbenchResourceCoordinator`'s admit/dispose lifecycle) got one small, real addition: an explicit correctness assertion after the 1,000-cycle loop (`afterLifecycle.leases !== 0 || afterLifecycle.accountedBytes !== 0` throws) — this is exactly the `PF10-1000-cycles` variant's declared `correctness: "retained-root-and-listener-counts"` check, which previously existed only implicitly (each cycle's own admission check, no final zero-leftover assertion). A new 30-trial runner, `bench/performance/t114-retention-trials.py` (mirrors `ledger-trials.py`'s pattern exactly: source-identity pinning, fresh subprocess per trial, discard-on-source-change), spawned 30 real independent Bun processes and computed `retained_growth_bytes` (RSS after − RSS before) per trial.

| Budget | Variant | Metric:statistic | Value | Bound | Assessment |
|---|---|---|---|---|---|
| RETENTION | PF10-1000-cycles | retained_growth:max | 3,801,088 bytes | < 10,485,760 bytes | within-target-diagnostic |

`tools/import-t114-retention.py` (same pattern as the PF12 importer) built and imported this as a `diagnostic`-classified observation. `PF10-pressure-eviction` (the budget's other declared variant, with a different `correctness: "retained-root-and-recovery-checksum"` requirement) is deliberately not touched — it was not measured here. `python3 tools/perf.py status` now reports `observed 4` (was 3, was 0 before this session). `tests/performance/test_retention_import.py` (3 tests) checks the bundle covers only the measured variant, imports cleanly, and rejects a failed correctness check or a fabricated `reference` classification. Full suite: 42/42 Python performance tests pass; `bun run check`, `bun run test:unit`, `python3 tools/plan.py check` all pass.

Retained artifacts: `.artifacts/performance/T114/retention-trials-2026-09-17.json` (raw 30-trial report), `.artifacts/performance/T114/retention-trials-2026-09-17-bundle.json` (imported bundle), `.artifacts/performance/T114/corpus/PF10-1000-cycles.bin` (materialized corpus backing `corpus_hash`).

This still does not move T106 toward done — `observed 4` of 1,016 is still overwhelmingly unproven, no certified reference host exists in this environment, and `producer.selector` remains `pending:T115:...` for every one of the 55 adapters (unchanged by this session; that stays T115's scope). The pattern is now demonstrated twice (PF12/LEDGER and PF10/RETENTION) and is mechanically repeatable for other already-buildable diagnostics — the actual bottleneck for further progress is dedicated hardware for reference-host classification, and T115's production wiring, not this pattern.

### Third attempt (IDLE): a real measurement, an over-target result, and a genuine catalog gap — not imported

A real, honest 60-second idle-CPU measurement was attempted for the `IDLE` budget (owner `platform`, ticket `T114`, the simplest declared `sample_policy`: `catalog-minimum-1-independent-trials`). `bench/performance/t114-idle-trial.py` spawns the actual production CLI under a real PTY, waits for its first ready frame, then samples `/proc/<pid>/stat` CPU ticks before and exactly 60 real seconds later with **zero input delivered** — a genuine idle window, not a synthetic proxy.

Result: `cpu_core_percent_mean = 1.0165%` — **over the budget's `<1%` bound**, on this shared, heavily-loaded VM (many concurrent `bun`/`tmux`/`Xvfb` processes were running from this session's other work at the time). This is reported exactly as measured; it was not adjusted, re-run selectively to get a better number, or discarded. Given the shared/noisy host, this single data point is not a reliable claim that Xi's real idle CPU exceeds budget — it is exactly the kind of ambiguous result the `reference_host`/`diagnostic` distinction exists to prevent from being release-qualifying. It was **not imported** into the ledger, for a separate, more fundamental reason described next.

**Genuine catalog gap found:** the `IDLE` budget's `workload` field reads "PF10 60s idle editor+workers; inactive external services separately reported", but its two declared `workload_variants` in `performance-adapters.json` are `PF10-1000-cycles` ("1,000 open/close/picker/view/contribution cycles") and `PF10-pressure-eviction` ("repeated pressure eviction and recovery") — **neither variant's description matches a 60-second idle-CPU measurement.** There is no declared PF10 variant for "just sit idle." Importing this measurement under either existing variant ID would mislabel what was actually measured (the exact anti-pattern this ticket's own remediation doc warns against: "One arbitrary PF fixture must not stand in for several required families"). This is left unimported and reported as a concrete, named gap for whoever does T115's catalog completion: either add a dedicated idle-workload variant (e.g. `PF10-idle-60s`) to `performance-adapters.json`'s `variant_catalog`, or clarify that one of the two existing variants' *setup* phase is followed by an idle window this budget actually measures. `bench/performance/t114-idle-trial.py` and its raw output (`.artifacts/performance/T114/idle-trial-2026-09-17.json`) are retained so this measurement does not need to be redone once the variant question is resolved — only re-imported under the correct variant ID.

### Fourth real observation: DOC-EOL (PF03, all four declared variants)

The same mechanical pattern was applied a fourth time, this time covering every declared variant of one budget rather than a single one. `bench/performance/t107-eol-metrics.ts` (a pre-existing, `diagnosticOnly: true` single-run diagnostic) opens 1 MiB/10 MiB uniform-LF and alternating-CRLF/LF fixtures and reads `TextFileDocument.lineEndingStorageMetrics().retainedBytes`. A new 30-trial runner, `bench/performance/t107-eol-trials.py` (same source-identity-pinning pattern as `ledger-trials.py`/`t114-retention-trials.py`), spawned 30 real independent Bun processes, and for each trial checked two real correctness properties per variant before accepting it: `exactSerializedBytes` (the document's own serialize round-trip succeeded) and the reported line-break count matches the fixture's own deterministic construction (within a 2-break tolerance for the final partial repeat unit at the fixture boundary).

| Budget | Variant | Metric:statistic | Value | Bound (`65536 + newlines*0.25`) | Assessment |
|---|---|---|---|---|---|
| DOC-EOL | PF03-1MiB-LF-dense | retained_eol:max | 5,632 bytes | 65,536 + 524,288×0.25 = 196,608 | within-target-diagnostic |
| DOC-EOL | PF03-1MiB-mixed-EOL | retained_eol:max | 109,434 bytes | 65,536 + 419,430×0.25 = 170,393.5 | within-target-diagnostic |
| DOC-EOL | PF03-10MiB-LF-dense | retained_eol:max | 56,320 bytes | 65,536 + 5,242,880×0.25 = 1,376,256 | within-target-diagnostic |
| DOC-EOL | PF03-10MiB-mixed-EOL | retained_eol:max | 1,093,632 bytes | 65,536 + 4,194,304×0.25 = 1,114,112 | within-target-diagnostic |

All four values were identical across all 30 trials (this is a deterministic scalar computation, not a timing measurement, so zero variance is the expected, correct result — not evidence the trial loop did nothing; the loop's real value is the source-identity guard and the per-trial correctness re-check, matching the established pattern's rigor even for a deterministic metric). `tools/import-t107-eol.py` (mirrors `tools/import-t114-retention.py`) built and imported all four as `diagnostic`-classified observations, each carrying the `newlines` parameter `tools/perf.py`'s bound formula (`max + newlines * per.newlines`) requires. `temporary_per_newline:max` (DOC-EOL's other declared metric — an H0 allocation-discipline check requiring real memory-profiling instrumentation around the line-ending construction loop) is deliberately **not** touched; this diagnostic does not attempt it. `python3 tools/perf.py status` now reports `observed 8` (was 4). `tests/performance/test_eol_import.py` (3 tests) checks the bundle covers exactly the four declared variants with the correct `newlines` parameters, imports cleanly with all four assessed `within-target-diagnostic`, and rejects a failed serialization-correctness check or a fabricated `reference` classification. Full Python performance suite: 45/45 pass (was 42). `bun run check`, `bun run test:unit` (32/32, was 31), `python3 tools/plan.py check` all pass.

Retained artifacts: `.artifacts/performance/T107/eol-trials-2026-09-17.json` (raw 30-trial report), `.artifacts/performance/T107/eol-trials-2026-09-17-bundle.json` (imported bundle), `.artifacts/performance/T107/corpus/PF03-*.bin` (the four materialized, hash-verified corpora backing each variant's `corpus_hash`).

This still does not move T106 toward done — `observed 8` of 1,016 remains overwhelmingly unproven, no certified reference host exists in this environment, and `producer.selector` remains `pending:T115:...` for all 55 adapters. It further demonstrates the pattern generalizes cleanly to a multi-variant budget in one pass, and to a metric requiring per-observation `parameters` for its bound formula (not just a flat threshold) — the mechanical bottleneck for continuing this pattern on the remaining ~1,008 declared cells is time, not a technical unknown; the actual **release** bottleneck remains unchanged: dedicated reference-host hardware for classification, and T115's production adapter wiring.
