# T056 — G4 language qualification

## Current status — reopened 2026-09-15

**Unproven / todo.** The G4 test mostly checks exported constructors and a snippet. Required actual CLI E07/E08/E14, real TS/Go/Rust servers and version/hash records are not established.

See [completion audit](2026-09-15-completion-audit.md). The original report below is historical component evidence; its earlier pass/completion statements do not establish current ticket completion. Preserve those results, implement the missing acceptance, and rerun the required checks before marking done.

- Ticket ID and title: T056 — G4 language qualification
- Outcome: passed for the composed language feature set
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T056-source-manifest.sha256](T056-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2; OpenTUI 0.5.11
- Specifications: `docs/plan/06-execution.md` G4 stop rule and `docs/plan/04-services.md`

## Observable result

The public services composition exposes transport-adjacent diagnostics, semantic tokens, completion, navigation, pull diagnostics, presentation features, snippets, workspace edits and formatting through the owned service APIs. The qualification fixture exercises the owners together and confirms the contracts remain versioned and bounded.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run tests/e2e/t056-g4.test.ts` | Passed diagnostics, navigation, completion, semantic-token, folding, snippet, workspace-edit and formatter owner composition checks. | `tests/e2e/t056-g4.test.ts`; fixture IDs `G4-*` |
| `bun x tsc --noEmit` | Passed. | Command output |
| `bun run check` | Passed pinned toolchain, strict TypeScript, public-boundary and architecture checks. | Command output |
| `bun run check:public-boundary` | Passed public DAG and architecture sentinels. | Command output |

## Performance and visual evidence

The fixture is a composition and contract gate. Request scheduling, large-document performance and terminal visual qualification remain owned by T061–T063.

## Failure cases and recovery

The participating services retain their stale-generation, invalid-payload, overlap and disposal guards. No Neovim runtime or second writable text store is introduced.

## Limitations and next action

This gate does not claim the full Vim parity, performance, visual or packaging release gates; those remain separate tickets.
