# Ticket evidence

- Ticket: T146 — theme picker, cursor guides, empty hover and split boundaries.
- Outcome: behavioral checks passed; mandatory performance qualification **unproven**, ticket remains in progress.
- Revision: working tree based on `f90db5c`; preserved pre-existing modifications. No commit or push.
- Environment: Linux arm64, Bun 1.3.13, TypeScript 7.0.2, pinned OpenTUI 0.5.11 fork. Xterm/Xvfb, DejaVu Sans Mono 10, 120×40.
- Specifications: UX editor refinements, E07/E11/E16, input/render ownership, specs 12/15.

## Observable result

Theme scopes no longer enable a full-height cursor column. Horizontal pane tabs share the split boundary: tab/close controls take priority and unused tab-strip space remains draggable. Vertical dividers remain. Empty or whitespace-only successful hovers close without a popup; pending/empty content is not rendered, and late replies do not reopen a closed popup.

The installed binary was dated September 20 at 01:10 CEST, older than the existing source fixes. The current source already renders the picker above a full-height sidebar; added a full-directory visual regression and verified Akari Night/Dawn preview as well as the existing interaction fixtures.

## Executed validation

| Command | Actual result |
|---|---|
| `bun run check` | Passed toolchain, strict types, architecture/public boundaries and lint after corrections. |
| `bun test tests/workbench/t116-language-overlays.test.ts tests/ui/pane-buffer-strips.test.ts tests/ui/t053-syntax-paint.test.ts tests/ui/theme-picker-layout.test.ts tests/ui/t063-contrast.test.ts tests/ui/t063-surface-matrix.test.ts` (included in the ten-file run) | Top-level assertions passed: empty/late hover; both split orientations, nested panes, tab and resize hits; no cursor-column color; picker rows/hover/scroll/resize; 69 contrast checks and surface matrix. |
| `python3 tests/e2e/theme-picker-visual.py` | Passed current theme, full sidebar, keyboard navigation, narrow resize and cancel; screenshots reviewed. |
| `python3 tests/e2e/t132-theme-switch-pty.py` | Passed real CLI preview/hover/cancel/apply, other surfaces and cross-process persistence. |
| `python3 tests/e2e/editor-refinements-visual.py` | Passed actual CLI save/delete/undo and inspected dark/light picker, cursor guides, horizontal/nested split screenshots. |
| `bun run package:build` | Compiled binary built successfully. |
| `XI_REFINEMENT_BINARY=/home/snowbear/projects/xi/dist/xi python3 tests/e2e/editor-refinements-visual.py` | Same actual-terminal journey passed with the compiled application. |
| `python3 tools/perf.py check` | Catalog valid: 55 budgets, 1,016 obligations; not measurement certification. |

These repository fixtures use top-level assertions; Bun's “0 tests” summary is not a count of those assertions.

## Performance and visual evidence

Screenshots: `.artifacts/ui/editor-refinements/{akari-editor,picker-over-sidebar,picker-light-preview,ghost,visual-adoption,horizontal-split,nested-split}.png`. User references downloaded with curl into `.artifacts/refinement-references/`. Reviewed the actual terminal screenshots, including distinct ghost/Visual colors and removal of the horizontal separator row.

H1/H2 UI/input work remains within the unchanged ENGINE-STEP / INPUT-OUTPUT / INPUT-LOADED parent contracts. Catalog hash: `1129c2799ffa38d6980cee5ad84d1f9bca9b16307f1ae3ce396f14b2fa25aa51`. No new service, animation or dependency. Full loaded CLI latency, physical latency, allocation, retained/native memory and paired Neovim performance are unmeasured here.

## Failure cases and recovery

Initial validation caught a Promise-renderer listener registration mistake and a function-length violation; both were corrected and checks rerun. The visual fixture initially tried saving TypeScript with an unavailable Biome formatter; it now uses plain text for exact-byte editing checks, leaving real formatter failure behavior intact. A malformed experimental fixture language override was removed. No production tests were disabled or budgets weakened.

## Limitations and next action

Reference-host output/physical/performance qualification remains open. Akari screenshots use read-only copies of installed public theme files; missing theme files fail the visual fixture. Private workspace data/configuration is not embedded in the fixture. See T147 for ghost semantics and its component timing evidence.

## Installed result

After the final compiled-CLI journey and `bun run check` passed, atomically replaced `~/.local/bin/xi` with `dist/xi` using a staged executable and rename. Preserved the previous binary at `.artifacts/refinement-references/xi-before-refinements`. `cmp dist/xi ~/.local/bin/xi` passed and `xi --version` returned `xi 0.0.1`; both files have SHA-256 `bd97f3569717191468aa8107d8b0b5c048a4e1908a689cc7619c41d4e514d06c`. Existing running processes keep their old executable until restarted.

## Goal completion audit

Revalidated the actual installed `~/.local/bin/xi` with `XI_REFINEMENT_BINARY=/home/snowbear/.local/bin/xi python3 tests/e2e/editor-refinements-visual.py`: passed exact saved delete/undo bytes and distinct ghost/Visual pixel checks. Re-inspected the regenerated picker, Akari editor, split and Visual screenshots. Reran the five relevant frame/controller/ghost assertion files; all passed. The installed and built binary hashes still match the digest above.

| Requested refinement | Current authoritative evidence |
|---|---|
| Read the three screenshots using curl | Retained downloaded PNGs in `.artifacts/refinement-references/`; inspected against the current captures. |
| Theme picker opened from editor | Installed CLI's full sidebar dark/light picker captures, plus passing theme-picker layout/interaction regressions. |
| Akari and other theme cursor-column stripe | Shared cursor-guide painter contains row guides only; scope-color regression and Akari capture pass. |
| No empty hover popup | Production hover surface requires ready, nonempty trimmed text; controller regressions pass empty/late/unavailable cases. |
| No extra horizontal split row | Shared split geometry allocates no horizontal separator row; frame/hit tests and installed horizontal/nested captures pass. |
| Last-motion ghost adopted by Visual mode | Engine-owned range/selection tests, strict-mode regressions, installed `w v d`/save/undo journey and distinct paint checks pass. |

This closes the requested functional refinements and local installation. T146/T147 retain their unproven release-performance qualification status; this audit does not promote them or claim native-level/physical-latency certification.
