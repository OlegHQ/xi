# T066 — G6 release qualification

- Ticket ID and title: T066 — G6 release qualification
- Outcome: unproven
- Implementation revision or tree hash: shared uncommitted worktree
- Source manifest: [T066-source-manifest.sha256](T066-source-manifest.sha256)
- Environment: Linux aarch64; Bun 1.3.13; TypeScript 7.0.2; OpenTUI 0.5.11; pinned Neovim 0.12.4 development oracle
- Specifications: `docs/plan/05-validation.md`, `docs/plan/08-selections.md`, `docs/plan/09-interaction.md`, `docs/plan/10-extensibility.md`

## Observable result

The implementation backlog has 99 of 105 tickets marked done, including G3 workbench composition, G4 language composition, G5 tooling/data integrity and multi-root identity. The source launcher now supports a document-backed Vim insert/motion/operator/register/put/save loop plus native Ex write/quit handling, and a compiled Linux ARM64 binary passes isolated launch, file:line, Ex, resize, quit and terminal restoration probes.

## Executed validation

| Command / fixture | Actual result | Evidence path |
|---|---|---|
| `bun run check` | Passed toolchain, strict TypeScript, boundary and architecture checks. | Command output |
| `bun run test:vim` | Passed the currently implemented pinned oracle/PTY fixture set. | `.artifacts/oracle/fixture-results.json` |
| `python3 tools/plan.py check` | Passed the 105-ticket dependency graph and report references. | Command output |
| `bun run verify:release` | Passed all currently registered check, unit, multi-selection, services, UI, interaction and benchmark fixtures (including 11 interaction fixtures and the 10,000-member selection benchmark). The runner still fails closed for a missing/empty required manifest or selector. | Command output; `tools/verify-suite.ts` |
| `bun run tests/e2e/t045-g3.test.ts`, `bun run tests/e2e/t056-g4.test.ts` | Passed G3/G4 composition fixtures. | `tests/e2e/` |
| `python3 tests/distribution/t064-editor-pty.py` | Passed production launcher Insert, Escape, Ctrl-S save and quit through a real PTY. | `tests/distribution/t064-editor-pty.py` |
| G5 service/data matrix (`t041`, `t042`, `t044`, `t058`, `t059`, `t060`, `t072`, `t073`, `t037`) | All focused fixtures passed. | T065 report |

## Performance and visual evidence

T016/T089 local probes and T087 screenshots are retained, but T061 parity, T062 full-system budgets, T063 complete UX/accessibility matrix, T064 complete distribution matrix and T089 long-window attribution remain unproven.

## Failure cases and recovery

The release command executes the retained fixture manifests and exposes missing required suites instead of returning a false pass. Current gate reports retain the exact unproven prerequisites and their artifacts.

## Limitations and next action

G6 cannot pass while T061, T062, T063, T064 or T089 remain unproven, or while mandatory platform, performance, visual and parity evidence is missing. The aggregate runner is green for the fixtures retained in this revision; the complete packaged no-Neovim PTY, performance/native comparison, visual/accessibility and parity matrices still need to run before release qualification.
