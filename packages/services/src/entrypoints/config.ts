/** Config file parsing loads only at startup, off the keystroke path. */
export { compileConfig, DEFAULT_CONFIG_TOML } from '../../config/index';
export type { CompiledConfig, ConfigLayer, ConfigCompileResult, LanguageConfig, LanguageServerConfig } from '../../config/index';
