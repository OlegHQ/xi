{
  "name": "xi",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.2 <2"
  },
  "scripts": {
    "check": "bun run check:toolchain && bun run check:types && bun run check:public-boundary && bun run check:config-ledger && bun run check:lint",
    "check:toolchain": "bun run ./tools/toolchain-check.ts",
    "check:types": "bun x tsc --noEmit",
    "check:public-boundary": "bun run ./tools/check-public-boundary.ts packages tools tests",
    "check:config-ledger": "bun run ./tools/check-config-ledger.ts",
    "test:unit": "bun run ./tools/verify-suite.ts unit \"$@\"",
    "test:vim": "bun run ./tests/oracle/validate.ts \"$@\"",
    "oracle:fetch": "bun run ./tests/oracle/fetch-neovim.ts",
    "test:services": "bun run ./tools/verify-suite.ts services \"$@\"",
    "test:ui": "bun run ./tools/verify-suite.ts ui \"$@\"",
    "test:e2e": "bun run ./tools/verify-suite.ts e2e \"$@\"",
    "bench": "bun run ./tools/verify-suite.ts bench \"$@\"",
    "xi": "bun run tools/xi-dev.ts",
    "xi:dev:build": "bun run tools/xi-dev.ts --build-only",
    "xi:dev": "bun run dist/dev/main.js",
    "skills:sync": "bun run ./tools/skills-sync.ts",
    "package:audit": "bun run ./tools/package-audit.ts --check",
    "package:build": "bun run tools/package-build.ts",
    "xi:install": "bun run package:build && mkdir -p ~/.local/bin && cp dist/xi ~/.local/bin/xi && chmod 755 ~/.local/bin/xi && ~/.local/bin/xi --version",
    "package:release": "bun run ./tools/package-release.ts",
    "package:smoke": "bun run package:audit && python3 tests/distribution/t064-compiled-pty.py && python3 tests/distribution/t064-ex-pty.py && bun test tests/distribution/t064-package.test.ts",
    "verify:release": "bun run check && bun run test:unit && bun run ./tools/verify-suite.ts vim --suite config && bun run test:vim -- --profile xi --suite multi-selection --seed 41027 && bun run test:services && bun run test:ui && bun run test:e2e -- --suite interaction && bun run bench -- --suite interaction",
    "check:lint": "bun run tools/lint/check.ts",
    "test:lint": "bun test --timeout 30000 tests/lint",
    "test:startup": "bun run package:build && bun test tests/distribution/t122-renderer-entry.test.ts && python3 tests/distribution/t122-startup-input.py && python3 tests/distribution/t053-syntax-compiled-pty.py"
  },
  "devDependencies": {
    "@opentui/core": "0.5.11",
    "@tree-sitter-grammars/tree-sitter-toml": "0.7.0",
    "@types/bun": "1.3.13",
    "@types/node": "26.5.1",
    "oxc-parser": "0.150.0",
    "oxlint": "1.83.0",
    "tree-sitter-json": "0.24.8",
    "tree-sitter-python": "0.25.0",
    "tree-sitter-ruby": "0.23.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@opentui/solid": "0.5.11",
    "solid-js": "1.9.12",
    "web-tree-sitter": "0.25.10",
    "vscode-jsonrpc": "8.2.1",
    "vscode-languageserver-protocol": "3.17.5"
  },
  "patchedDependencies": {
    "@opentui/core@0.5.11": "vendor/opentui/patches/opentui-core-0.5.11.patch",
    "@opentui/solid@0.5.11": "vendor/opentui/patches/opentui-solid-0.5.11.patch"
  }
}
